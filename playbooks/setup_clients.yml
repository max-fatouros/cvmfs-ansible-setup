- hosts:
    - proxies
    - stratum_1s
  gather_facts: true

- name: Setup CVMFS Stratum 1
  hosts: stratum_1s
  gather_facts: true
  become: true
  collections:
    - cern.cvmfs
  roles:
    - cern.cvmfs.common
    - cern.cvmfs.clients
